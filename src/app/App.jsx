import React from 'react';
import { BrowserRouter as Router, Route, Routes } from 'react-router-dom';

/* para poner rutas a
const App = () => {
    return (
        <Router>
            <Routes>
                <Route path="/" element={} />
            </Routes>
        </Router>
    );
};
*/
export default App;